import { InputReferenceDirective } from "./input-reference.directive";

describe("InputReferenceDirective", () => {
  it("should create an instance", () => {
    const directive = new InputReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
